import React from "react"; import { View, Text, ScrollView, TouchableOpacity } from "react-native"; import { Card } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { useNavigation } from "@react-navigation/native";

const exercises = [ { id: 1, name: "Squat", description: "Esercizio per gambe e glutei." }, { id: 2, name: "Panca Piana", description: "Esercizio per pettorali e tricipiti." }, { id: 3, name: "Stacchi da terra", description: "Esercizio completo per tutto il corpo." }, ];

export default function PersonalTrainerApp() { const navigation = useNavigation();

return ( <ScrollView className="p-4"> <Text className="text-xl font-bold mb-4">Programma di Allenamento</Text> {exercises.map((exercise) => ( <Card key={exercise.id} className="mb-4 p-4"> <Text className="text-lg font-semibold">{exercise.name}</Text> <Text className="text-gray-600">{exercise.description}</Text> <Button className="mt-2" onPress={() => navigation.navigate("Dettaglio", { exercise })}> Dettagli </Button> </Card> ))} <Button className="mt-4" onPress={() => navigation.navigate("SchedaAllenamento")}>Scheda di Allenamento</Button> <Button className="mt-4" onPress={() => navigation.navigate("MonitoraggioProgresso")}>Monitoraggio Progresso</Button> </ScrollView> ); }

